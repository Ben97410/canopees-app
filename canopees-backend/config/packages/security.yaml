security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        # Used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false

        # API firewall: stateless and session-free to prevent conflict with sessions
        api:
            pattern: ^/api
            stateless: true
            
            
            
            
            

        main:
            lazy: true
            provider: app_user_provider
            custom_authenticator: App\Security\AppLoginFormAuthenticator
            entry_point: App\Security\AppLoginFormAuthenticator
            logout:
                path: app_logout
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800
                path: /
                always_remember_me: false

    # Note: Only the *first* matching rule is applied

    access_control:

   # 1. Allow public read access (GET) on the entire API
        - { path: ^/api, roles: PUBLIC_ACCESS, methods: [GET] }
        
        # 2. Allow public creation (POST) for quote requests
        - { path: ^/api/demande_devis, roles: PUBLIC_ACCESS, methods: [POST] }
        
        # 3. Require ROLE_ADMIN for write operations (POST, PUT, DELETE, PATCH)
        # We target specific paths to ensure public GETs are not blocked
        - { path: ^/api/oeuvres, roles: ROLE_ADMIN, methods: [POST, PUT, DELETE, PATCH] }
        
        # 4. Protect the admin interface
        - { path: ^/admin, roles: ROLE_ADMIN }
        
when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon